title: HTML AND CSS
description: 'Learn Basic html and css '
template:
  repo: html-and-css-template
  name: html-and-css
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
